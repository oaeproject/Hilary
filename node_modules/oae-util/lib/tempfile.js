/*!
 * Copyright 2012 Sakai Foundation (SF) Licensed under the
 * Educational Community License, Version 2.0 (the "License"); you may
 * not use this file except in compliance with the License. You may
 * obtain a copy of the License at
 * 
 *     http://www.osedu.org/licenses/ECL-2.0
 *
 * Unless required by applicable law or agreed to in writing,
 * software distributed under the License is distributed on an "AS IS"
 * BASIS, WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express
 * or implied. See the License for the specific language governing
 * permissions and limitations under the License.
 */

var fs = require('fs');
var Path = require('path');
var Temp = require('temp');

var log = require('oae-logger').logger('TempFile');

/**
 * A model to represent temporary files.
 *
 * @param {String} path The path to the temp file.
 * @param {Number} size The filesize of this temporary file in bytes.
 */
var TempFile = module.exports.TempFile = function(path, size) {
    var that = {};
    that.path = path;
    that.size = size;
    that.name = Path.basename(path);

    /**
     * Remove the temporary file.
     *
     * @param {Function}    callback        Standard callback method.
     * @param {Object}      callback.err    An error object in case the file could not be removed.
     */
    that.remove = function(callback) {
        fs.unlink(that.path, function(err) {
            if (err) {
                log().error({'err': err}, 'Could not remove temporary file: %s', that.path);
                return callback({'code': 500, 'msg': 'Could not remove temporary file ' + that.path});
            }
            callback();
        });
    };

    /**
     * Updates the metadata of a temp file.
     *
     * @param {Function}    callback        Standard callback method.
     * @param {Object}      callback.err    An error object in case the latest metadata could not be retrieved
     * @param {Object}      callback.file   Returns the updated temp file (a reference to it self.)
     */
    that.update = function(callback) {
        fs.stat(that.path, function(err, stat) {
            if (err) {
                log().error({'err': err}, 'Could not stat the file: %s', that.path);
                return callback({'code': 500, 'msg': err});
            }

            that.size = stat.size;
            callback(null, that);
        });
    };

    return that;
};

/**
 * Create a new temporary file. The temp path generation is a wrapper around Bruce Williams node-temp's `Temp.path` method.
 * You can pass in optional suffices.
 *
 * @see https://github.com/bruce/node-temp
 * @param  {Object}     [options]   An option suffix
 * @return {TempFile}
 */
var createTempFile = module.exports.createTempFile = function(options) {
    options = options || {};
    var tempPath = Temp.path(options);
    return new TempFile(tempPath, 0);
};
