var Fields = require('oae-config/lib/fields');

module.exports.config = {
    "oae-tenants": {
        "title": "OAE Tenant Module",
        "allow-tenant-stop": {
            "name": "Stop Tenant Server",
            "description": "Allow a tenant to stop the tenant server",
            "elements": {
                "tenantstop": new Fields.Boolean('Stop tenant', 'Allow a tenant to stop the tenant server', false, false)
            }
        },
        "allow-tenant-change-details": {
            "name": "Tenant Details",
            "description": "Allow a tenant to change the details of the instance",
            "elements": {
                "changedetails": new Fields.Boolean('Change tenant details', 'Allow a tenant to change the details of the tenant server', false, false)
            }
        }
    }
}
