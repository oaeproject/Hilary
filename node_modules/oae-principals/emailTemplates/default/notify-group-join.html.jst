<% var user = data.activity.actor %>
<% var group = data.activity.object %>
<p>
    Hi,
</p>
<p>
    <%= util.sanitization.encodeForHTML(user.displayName) %> has joined a group that you manage: "<%= util.sanitization.encodeForHTML(group.displayName) %>".
</p>
<p>
    You may find it by following this link:
    <a href="<%= group.url %>" title="Go to <%= util.sanitization.encodeForHTMLAttribute(group.displayName) %>">
        <%= group.url %>
    </a>
</p>
