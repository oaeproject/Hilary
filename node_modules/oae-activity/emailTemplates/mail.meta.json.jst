<%
var subject = '__MSG__RECENT_ACTIVITY__';
if (activities.length === 1) {
    subject = util.translate(activities[0].summary.i18nKey, activities[0].summary.i18nArguments);
}
%>
{
    "subject": "<%= util.trim(util.jsonSafe(util.htmlToText(subject))) %>"
}