/*!
 * Copyright 2014 Apereo Foundation (AF) Licensed under the
 * Educational Community License, Version 2.0 (the "License"); you may
 * not use this file except in compliance with the License. You may
 * obtain a copy of the License at
 *
 *     http://opensource.org/licenses/ECL-2.0
 *
 * Unless required by applicable law or agreed to in writing,
 * software distributed under the License is distributed on an "AS IS"
 * BASIS, WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express
 * or implied. See the License for the specific language governing
 * permissions and limitations under the License.
 */

var _ = require('underscore');
var OAE = require('oae-util/lib/oae');

var TenantsAPI = require('oae-tenants/lib/api');
var TenantNetworksAPI = require('oae-tenants/lib/api.networks');

//////////////////////////////////
// GLOBAL ADMIN TENANT NETWORKS //
//////////////////////////////////

/**
 * @REST getTenantNetworks
 *
 * Get all the tenant networks in the system
 *
 * @Server      admin
 * @Method      GET
 * @Path        /tenantNetworks
 * @Return      {TenantNetworks}                Returns an object of tenant networks and their associated tenants
 */
OAE.globalAdminRouter.on('get', '/api/tenantNetworks', function(req, res) {
    TenantNetworksAPI.getTenantNetworks(req.ctx, function(err, tenantNetworks) {
        if (err) {
            return res.send(err.code, err.msg);
        }

        return res.send(200, tenantNetworks);
    });
});

/**
 * @REST postTenantNetworkCreate
 *
 * Create a tenant network
 *
 * @Server      admin
 * @Method      POST
 * @Path        /tenantNetwork/create
 * @BodyParam   {TenantNetworkInfo}     body    Object describing new tenant network to create
 * @Return      {TenantNetwork}                 Returns (via HTTP status 201) object representing newly created network
 */
OAE.globalAdminRouter.on('post', '/api/tenantNetwork/create', function(req, res) {
    TenantNetworksAPI.createTenantNetwork(req.ctx, req.body.displayName, function(err, tenantNetwork) {
        if (err) {
            return res.send(err.code, err.msg);
        }

        return res.send(201, tenantNetwork);
    });
});

/**
 * @REST postTenantNetworkId
 *
 * Update a tenant network
 *
 * @Server      admin
 * @Method      POST
 * @Path        /tenantNetwork/{id}
 * @PathParam   {string}                id              Identifier of network to update
 * @BodyParam   {TenantNetworkInfo}     body            Updated tenant network information
 * @Return      {TenantNetwork}                         Returns object representing updated network
 */
OAE.globalAdminRouter.on('post', '/api/tenantNetwork/:id', function(req, res) {
    TenantNetworksAPI.updateTenantNetwork(req.ctx, req.params.id, req.body.displayName, function(err, tenantNetwork) {
        if (err) {
            return res.send(err.code, err.msg);
        }

        return res.send(200, tenantNetwork);
    });
});

/**
 * @REST deleteTenantNetworkId
 *
 * Delete a tenant network
 *
 * @Server      admin
 * @Method      DELETE
 * @Path        /tenantNetwork/{id}
 * @PathParam   {string}                id      Identifier of network to delete
 */
OAE.globalAdminRouter.on('delete', '/api/tenantNetwork/:id', function(req, res) {
    TenantNetworksAPI.deleteTenantNetwork(req.ctx, req.params.id, function(err) {
        if (err) {
            return res.send(err.code, err.msg);
        }

        return res.send(200);
    });
});

/**
 * @REST postTenantNetworkIdAddTenants
 *
 * Add tenants to a tenant network
 *
 * @Server      admin
 * @Method      POST
 * @Path        /tenantNetwork/{id}/addTenants
 * @PathParam   {string}                        id          Identifier of network for tenants
 * @BodyParam   {TenantAliases}                 body        Tenant(s) to add to network
 */
OAE.globalAdminRouter.on('post', '/api/tenantNetwork/:id/addTenants', function(req, res) {
    var tenantAliases = _.isString(req.body.alias) ? [req.body.alias] : req.body.alias;
    TenantNetworksAPI.addTenantAliases(req.ctx, req.params.id, tenantAliases, function(err) {
        if (err) {
            return res.send(err.code, err.msg);
        }

        return res.send(200);
    });
});

/**
 * @REST postTenantNetworkIdRemoveTenants
 *
 * Remove tenants from a tenant network
 *
 * @Server      admin
 * @Method      POST
 * @Path        /tenantNetwork/{id}/removeTenants
 * @PathParam   {string}                        id          Identifier of network for tenants
 * @BodyParam   {TenantAliases}                 body        Tenant(s) to remove from network
 */
OAE.globalAdminRouter.on('post', '/api/tenantNetwork/:id/removeTenants', function(req, res) {
    var tenantAliases = _.isString(req.body.alias) ? [req.body.alias] : req.body.alias;
    TenantNetworksAPI.removeTenantAliases(req.ctx, req.params.id, tenantAliases, function(err) {
        if (err) {
            return res.send(err.code, err.msg);
        }

        return res.send(200);
    });
});


//////////////////////////
// GLOBAL ADMIN TENANTS //
//////////////////////////

/**
 * @REST getTenants
 *
 * Get a list of all available tenants
 *
 * @Server      admin,tenant
 * @Method      GET
 * @Path        /tenants
 * @Return      {Tenants}                Returns an object of tenant networks and their associated tenants
 */
OAE.globalAdminRouter.on('get', '/api/tenants', function(req, res, next) {
    var tenants = TenantsAPI.getTenants();
    res.send(200, tenants);
});

/**
 * @REST getTenant
 *
 * Get the metadata of the current tenant
 *
 * @Server      admin
 * @Method      GET
 * @Path        /tenant
 * @Return      {Tenant}                        Returns information about the tenant
 */
OAE.globalAdminRouter.on('get', '/api/tenant', function(req, res) {
    res.send(200, req.ctx.tenant());
});

/**
 * @REST getTenantAlias
 *
 * Get a tenant's metadata
 *
 * @Server      admin
 * @Method      GET
 * @Path        /tenant/{alias}
 * @PathParam   {string}           alias        Unique name of tenant to return
 * @Return      {Tenant}                        Returns information about the tenant
 */
OAE.globalAdminRouter.on('get', '/api/tenant/:alias', function(req, res) {
    var tenant = TenantsAPI.getTenant(req.params.alias);
    if (!tenant) {
        return res.send(404, 'There is no tenant with alias ' + req.params.alias);
    }
    res.send(200, tenant);
});

/**
 * @REST postTenantCreate
 *
 * Create a new tenant
 *
 * @Server      admin
 * @Method      POST
 * @Path        /tenant/create
 * @BodyParam   {NewTenant}         body            Information about tenant to create
 */
OAE.globalAdminRouter.on('post', '/api/tenant/create', function(req, res) {
    TenantsAPI.createTenant(req.ctx, req.body.alias, req.body.displayName, req.body.host, function(err, tenant) {
        if (err) {
            return res.send(err.code, err.msg);
        }
        res.send(200, tenant);
    });
});

/**
 * @REST postTenantStart
 *
 * Start an existing tenant or tenants
 *
 * @Server      admin
 * @Method      POST
 * @Path        /tenant/start
 * @BodyParam   {TenantAliases}     body     Unique name(s) of tenant(s) to start
 */
OAE.globalAdminRouter.on('post', '/api/tenant/start', function(req, res) {
    // Sets the tenant to be ENABLED by passing in 'false'
    TenantsAPI.disableTenants(req.ctx, req.body.aliases, false, function(err) {
        if (err) {
            return res.send(err.code, err.msg);
        }
        res.send(200);
    });
});

/**
 * @REST postTenantStop
 *
 * Stop an existing tenant or tenants
 *
 * @Server      admin
 * @Method      POST
 * @Path        /tenant/stop
 * @BodyParam   {TenantAliases}     body     Unique name(s) of tenant(s) to stop
 */
OAE.globalAdminRouter.on('post', '/api/tenant/stop', function(req, res) {
    // Sets the tenant to be disabled by passing in 'true'
    TenantsAPI.disableTenants(req.ctx, req.body.aliases, true, function(err) {
        if (err) {
            return res.send(err.code, err.msg);
        }
        res.send(200);
    });
});

/**
 * @REST postTenantDelete
 *
 * Delete an existing tenant or tenants
 *
 * @Server      admin
 * @Method      POST
 * @Path        /tenant/delete
 * @BodyParam   {TenantAliases}     body     Unique name(s) of tenant(s) to delete
 */
OAE.globalAdminRouter.on('post', '/api/tenant/delete', function(req, res) {
    TenantsAPI.deleteTenants(req.ctx, req.body.aliases, function(err) {
        if (err) {
            return res.send(err.code, err.msg);
        }
        res.send(200);
    });
});

/**
 * @REST postTenantAlias
 *
 * Update a tenant's metadata
 *
 * @Server      admin
 * @Method      POST
 * @Path        /tenant/{alias}
 * @PathParam   {string}            alias           Unique name of tenant to modify
 * @BodyParam   {TenantUpdate}      tenantInfo      New tenant information (only include changed properties)
 */
OAE.globalAdminRouter.on('post', '/api/tenant/:alias', function(req, res) {
    TenantsAPI.updateTenant(req.ctx, req.params.alias, req.body, function(err) {
        if (err) {
            return res.send(err.code, err.msg);
        }
        res.send(200);
    });
});


/////////////
// Tenants //
/////////////

OAE.tenantRouter.on('get', '/api/tenant', function(req, res) {
    var tenant = TenantsAPI.getTenant(req.ctx.tenant().alias);
    if (!tenant) {
        return res.send(404, 'There is no tenant with alias ' + req.ctx.tenant().alias);
    }
    res.send(200, tenant);
});

/**
 * @REST postTenant
 *
 * Update the current tenant's metadata
 *
 * @Server      tenant
 * @Method      POST
 * @Path        /tenant
 * @BodyParam   {TenantUpdate}      tenantInfo      New tenant information (only include changed properties)
 */
OAE.tenantRouter.on('post', '/api/tenant', function(req, res) {
    TenantsAPI.updateTenant(req.ctx, req.ctx.tenant().alias, req.body, function(err) {
        if (err) {
            return res.send(err.code, err.msg);
        }
        res.send(200);
    });
});


// Add the REST models for Tenants


/**
 * @RESTModel TenantNetworkInfo
 *
 * @Required    [displayName]
 * @Property    {string}            displayName         Display name for tenant network
 */

/**
 * @RESTModel TenantAlias
 *
 * @Required    [alias]
 * @Property    {string}            alias               Tenant or array of tenants identified by short alias
 */

/**
 * @RESTModel TenantAliases
 *
 * @Required    [alias]
 * @Property    {string}            aliases             Tenant or array of tenants identified by short alias
 */

/**
 * @RESTModel TenantNetworks
 *
 * @Required  []
 * @Property  {TenantNetwork}       {tenantId}          Tenant network for named tenant Identifier
 */

/**
 * @RESTModel TenantNetwork
 *
 * @Required  [displayName, id]
 * @Property  {string}              displayName         Display name for tenant network
 * @Property  {string}              id                  Unique identifier for tenant network
 * @Property  {Tenant[]}            tenants             Tenants associated with tenant network
 */

/**
 * @RESTModel NewTenant
 *
 * @Required  [alias, displayName, host]
 * @Property  {string}            alias           Unique short name for tenant in URLs (should be lowercase)
 * @Property  {string}            displayName     Display name for tenant
 * @Property  {string}            host            Host for tenant
 */

/**
 * @RESTModel Tenants
 *
 * @Required  []
 * @Property  {Tenant[]}        tenants         Tenants
 */

/**
 * @RESTModel TenantUpdate
 *
 * @Required  []
 * @Property  {string}          displayName             Display name for tenant
 * @Property  {string}          host                    Host address for tenant
 */


