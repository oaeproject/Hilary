<p>
    Bonjour,
</p>
<p>
    <%= sanitization.encodeForHTML(data.activity.actor.displayName) %> a mis à jour "<%= sanitization.encodeForHTML(data.activity.object.displayName) %>".
</p>
<p>
    Vous pouvez y accéder en suivant ce lien :
    <a href="<%= sanitization.encodeForURL(data.activity.object.url) %>" title="Accéder à <%= sanitization.encodeForHTML(data.activity.object.displayName) %>">
        <%= sanitization.encodeForURL(data.activity.object.url) %>
    </a>
</p>
