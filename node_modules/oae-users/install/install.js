var OAE = require("oae-util/lib/OAE");

OAE.runQuery("CREATE TABLE users (KEY uuid PRIMARY KEY, tenantId text, institutionalId text, firstName text, lastName text, email text, " + 
             "picture text) WITH comment='user information' AND read_repair_chance = 1.0;");
