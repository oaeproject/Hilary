<p>
    Hi,
</p>
<p>
    <%= sanitization.encodeForHTML(data.activity.actor.displayName) %> has shared "<%= sanitization.encodeForHTML(data.activity.object.displayName) %>" with you.
</p>
<p>
    You may find it by following this link:
    <a href="<%= sanitization.encodeForURL(data.activity.object.url) %>" title="Go to <%= sanitization.encodeForHTML(data.activity.object.displayName) %>">
        <%= sanitization.encodeForURL(data.activity.object.url) %>
    </a>
</p>
