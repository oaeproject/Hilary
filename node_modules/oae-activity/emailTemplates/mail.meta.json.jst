<%
var subject = '__MSG__RECENT_ACTIVITY__';
if (activities.length === 1) {
    subject = util.i18n.translate(activities[0].summary.i18nKey, activities[0].summary.i18nArguments);
}
%>
{
    "subject": "<%= util.text.trim(util.json.escape(util.html.toText(subject))) %>"
}
