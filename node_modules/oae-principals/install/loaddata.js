var api = require('oae-principals');

var NR_OF_USERS = ;

var id = Math.floor(Math.random()*1000000);
var tenant = {'id': 'cam'};

var created = 0;
var done = function(err) {
    created++;
    console.log("Created user: " + created);
    if (created === NR_OF_USERS) {
        console.timeEnd("load-users");
    }
};

console.time("load-users");
for (var i = 0; i < NR_OF_USERS;i++) {
    var username = "username-" + id + "-i";
    var firstname = "username-" + id + "-i";
    var lastname = "username-" + id + "-i";
    var displayname = firstname + " " + lastname;

    api.createUser(tenant, username, "password", "loggedin", firstname, lastname, displayname, done);
}