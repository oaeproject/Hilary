<p>
    Hi,
</p>
<p>
    <%= util.sanitization.encodeForHTML(data.activity.actor.displayName) %> has shared "<%= util.sanitization.encodeForHTML(data.activity.object.displayName) %>" with you.
</p>
<p>
    You may find it by following this link:
    <a href="<%= data.activity.object.url %>" title="Go to <%= util.sanitization.encodeForHTMLAttribute(data.activity.object.displayName) %>">
        <%= data.activity.object.url %>
    </a>
</p>
