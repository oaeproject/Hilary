<%
    var subject = shared.getEmailSubject(util, recipient, activities);

    // Convert it to text in case there was HTML in the translations
    subject = util.html.toText(subject);

    // Truncate it so we don't max out the subject line
    subject = util.text.truncate(subject, 150);

    // Ensure it's safe to embed in a JSON value
    subject = util.json.escape(subject);
%>
{
    "subject": "<%= subject %>"
}
