<p>
    Bonjour,
</p>
<p>
    <%= util.sanitization.encodeForHTML(data.activity.actor.displayName) %> a partagé "<%= util.sanitization.encodeForHTML(data.activity.object.displayName) %>" avec vous.
</p>
<p>
    Vous pouvez y accéder en suivant ce lien :
    <a href="<%= data.activity.object.url %>" title="Accéder à <%= util.sanitization.encodeForHTMLAttribute(data.activity.object.displayName) %>">
        <%= data.activity.object.url %>
    </a>
</p>
<p>
     Ce mail est une notification automatique, veuillez ne pas y répondre.
</p>
