var Cassandra = require('oae-util/lib/cassandra');

module.exports = function(config, callback){
    Cassandra.createColumnFamilies({
        'Annotations': 'CREATE COLUMNFAMILY Annotations (revisionId text PRIMARY KEY)'
    });
    callback();    
}
