var Fields = require('oae-config/lib/fields');

module.exports = {
    "oae-authentication": {
        "title": "OAE Authentication Module",
        "authentication": {
            "name": "Account creation, login and logout",
            "description": "Account creation, login and logout",
            "tenantOverride": true,
            "elements": {
                "followlogoutredirects": new Fields.Boolean('Follow logout redirects', 'Follow logout redirects. Set to true if you have an authentication system such as CAS that needs to redirect to the user\'s browser on logout', false, true)
            }
        }
    }
}
